import Link from 'next/link';import SectionHead from '@/components/common/SectionHead';import { matches } from '@/data/siteData';
export default function MatchesPreview(){return <section className="section" style={{background:'#fff'}}><div className="container"><SectionHead eyebrow="Matches" title="Upcoming & recent matches" text="Frontend-only cards; later we can connect these to your PHP/Laravel/Node API."/><div className="cards-3">{matches.map(m=><div className="match-card reveal" key={m.id}><span className="badge">{m.status}</span><h3>{m.title}</h3><p>{m.date} • {m.venue}</p><div className="score">{m.score}</div></div>)}</div><div style={{textAlign:'center',marginTop:30}}><Link className="btn btn-primary" href="/matches">All Matches</Link></div></div></section>}
